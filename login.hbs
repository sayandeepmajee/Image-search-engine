<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | SoundRealm</title>
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #222, #555);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    .login-container {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 16px;
      backdrop-filter: blur(8px);
      box-shadow: 0 4px 30px rgba(0,0,0,0.3);
      padding: 40px 50px;
      width: 320px;
      text-align: center;
    }
    h1 {
      margin-bottom: 20px;
      font-size: 28px;
      letter-spacing: 1px;
      color: #f4f4f4;
    }
    input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.2);
      color: white;
      font-size: 16px;
      outline: none;
      transition: background 0.3s;
    }
    input:focus {
      background: rgba(255, 255, 255, 0.3);
    }
    button {
      width: 100%;
      padding: 12px;
      background: #ffb800;
      border: none;
      border-radius: 8px;
      color: #222;
      font-weight: 600;
      font-size: 16px;
      cursor: pointer;
      margin-top: 10px;
      transition: background 0.3s ease;
    }
    button:hover {
      background: #ffcf40;
    }
    p {
      margin-top: 15px;
      font-size: 14px;
      color: #ddd;
    }
    a {
      color: #ffb800;
      text-decoration: none;
      font-weight: 500;
    }
    a:hover {
      text-decoration: underline;
    }
    .note {
      font-size: 13px;
      color: #ffcf40;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Welcome Back</h1>

    {{#if unverified}}
      <p class="note">Your email is not verified yet. Please check your inbox for OTP.</p>
    {{/if}}

    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="Enter your Email" required>
      <input type="password" name="password" placeholder="Enter your Password" required>
      <button type="submit">Log In</button>
    </form>

    <p>Don’t have an account? <a href="/signup">Sign up</a></p>
  </div>

  {{#if error}}
  <script>
    alert("{{error}}");
  </script>
  {{/if}}
</body>
</html>
